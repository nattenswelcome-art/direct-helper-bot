# 🖥️ Работа с терминалом и навигация

## 📂 Как перейти в папку проекта

### Вариант 1: Через команду `cd` (Command Line)

```bash
# Открыть Terminal (Терминал)
# macOS: Cmd + Space → Terminal
# или Applications → Utilities → Terminal

# Перейти в папку проекта
cd /Users/tikhomirovevg/Direct_helper_bot

# Проверить что вы в правильной папке
pwd
# Должно показать: /Users/tikhomirovevg/Direct_helper_bot

# Посмотреть содержимое папки
ls -la
```

### Вариант 2: Через VSCode (самый простой)

```
1. Открыть VSCode
2. File → Open Folder...
3. Выбрать: /Users/tikhomirovevg/Direct_helper_bot
4. Открыть встроенный терминал: Ctrl + ` (или View → Terminal)
```

**Терминал VSCode автоматически откроется в папке проекта!** ✨

### Вариант 3: Перетащить папку в Terminal (macOS)

```
1. Открыть Finder
2. Найти папку: /Users/tikhomirovevg/Direct_helper_bot
3. Открыть Terminal
4. Набрать: cd 
5. Перетащить папку из Finder в Terminal
6. Нажать Enter
```

---

## 🎯 Основные команды терминала

### Навигация:

```bash
# Показать текущую папку
pwd

# Перейти в домашнюю папку
cd ~
# или просто
cd

# Перейти в папку проекта
cd /Users/tikhomirovevg/Direct_helper_bot

# Перейти на уровень выше
cd ..

# Перейти в подпапку
cd plans

# Вернуться в предыдущую папку
cd -
```

### Просмотр содержимого:

```bash
# Показать файлы и папки
ls

# Показать с деталями (размер, дата, права)
ls -l

# Показать все файлы (включая скрытые .gitignore, .env)
ls -la

# Показать только папки
ls -d */

# Показать в виде дерева (если установлен tree)
tree
# или
tree -L 2  # только 2 уровня
```

### Работа с файлами:

```bash
# Создать файл
touch filename.txt

# Создать папку
mkdir folder_name

# Создать вложенные папки
mkdir -p src/bot/handlers

# Скопировать файл
cp source.txt destination.txt

# Переместить/переименовать
mv old_name.txt new_name.txt

# Удалить файл
rm filename.txt

# Удалить папку
rm -rf folder_name  # ОСТОРОЖНО! Удаляет безвозвратно

# Просмотреть содержимое файла
cat filename.txt

# Открыть файл в редакторе
nano filename.txt
# или
code filename.txt  # откроет в VSCode
```

---

## 🚀 Быстрый старт для вашего проекта

### Способ 1: Через VSCode (рекомендуется)

```
1. Открыть VSCode
2. File → Open Folder...
3. Выбрать: Direct_helper_bot
4. Открыть терминал: Ctrl + ` (или Terminal → New Terminal)
5. Вы автоматически в папке проекта!
```

Проверка:
```bash
pwd
# Должно показать: /Users/tikhomirovevg/Direct_helper_bot

ls -la
# Должны увидеть: .gitignore, plans/, и т.д.
```

### Способ 2: Через обычный Terminal

```bash
# 1. Открыть Terminal (Cmd + Space → Terminal)

# 2. Перейти в проект
cd /Users/tikhomirovevg/Direct_helper_bot

# 3. Проверка
pwd
ls -la

# 4. Открыть VSCode из терминала (если нужно)
code .
```

---

## 📝 Полезные сочетания клавиш в Terminal

```
Tab               - Автодополнение команд и путей
Ctrl + C          - Прервать выполнение команды
Ctrl + D          - Выход из терминала
Ctrl + L          - Очистить экран (или команда: clear)
Ctrl + A          - Переместить курсор в начало строки
Ctrl + E          - Переместить курсор в конец строки
Ctrl + U          - Удалить все до курсора
Ctrl + K          - Удалить все после курсора
Ctrl + W          - Удалить слово перед курсором
↑ / ↓             - Листать историю команд
```

---

## 🎯 Проверка что вы в правильной папке

Выполните эти команды для проверки:

```bash
# 1. Показать текущую папку
pwd
# Ожидается: /Users/tikhomirovevg/Direct_helper_bot

# 2. Показать содержимое
ls -la
# Вы должны увидеть:
# .gitignore
# plans/
# direct-helper-architecture.md
# google-sheets-integration.md
# и другие файлы

# 3. Проверить Git
git status
# Если репозиторий инициализирован, покажет статус
# Если нет, напишет "not a git repository"
```

---

## 🔧 Настройка Terminal для удобства (опционально)

### Добавить алиас для быстрого перехода:

```bash
# Открыть файл конфигурации
nano ~/.zshrc
# или для bash:
nano ~/.bash_profile

# Добавить в конец файла:
alias dh='cd /Users/tikhomirovevg/Direct_helper_bot'

# Сохранить: Ctrl + O, Enter, Ctrl + X

# Применить изменения:
source ~/.zshrc
# или
source ~/.bash_profile

# Теперь можно использовать:
dh  # перейдет в папку проекта сразу!
```

---

## 📚 Полезные команды Git (после перехода в проект)

```bash
# Проверить статус
git status

# Посмотреть изменения
git diff

# Добавить все файлы
git add .

# Создать коммит
git commit -m "Описание изменений"

# Push на GitHub
git push

# Посмотреть историю
git log --oneline

# Посмотреть ветки
git branch

# Создать новую ветку
git checkout -b feature/new-feature
```

---

## ✅ Чек-лист для начала работы

Откройте Terminal и выполните:

```bash
# 1. Перейти в проект
cd /Users/tikhomirovevg/Direct_helper_bot

# 2. Проверить текущую папку
pwd

# 3. Посмотреть файлы
ls -la

# 4. Проверить Git
git status

# 5. Если Git не инициализирован:
git init
git add .
git commit -m "Initial commit"

# 6. Открыть в VSCode (опционально)
code .
```

---

## 🆘 Troubleshooting

### Проблема: "command not found: cd"
**Решение:** cd - встроенная команда, всегда работает. Проверьте что вы в Terminal, а не в другом приложении.

### Проблема: "No such file or directory"
**Решение:** Проверьте путь. Попробуйте:
```bash
ls /Users/tikhomirovevg/
# Должна быть папка Direct_helper_bot в списке
```

### Проблема: "Permission denied"
**Решение:** Добавьте `sudo` перед командой (для административных действий):
```bash
sudo command_name
# Введет пароль macOS
```

### Проблема: Терминал завис
**Решение:** Нажмите Ctrl + C для прерывания команды

---

## 🎓 Дополнительные ресурсы

- [Terminal User Guide (Apple)](https://support.apple.com/guide/terminal/welcome/mac)
- [Command Line Crash Course](https://learnpythonthehardway.org/book/appendixa.html)
- [Git Handbook](https://guides.github.com/introduction/git-handbook/)

---

Готово! Теперь вы знаете как работать с Terminal и Git! 🚀
